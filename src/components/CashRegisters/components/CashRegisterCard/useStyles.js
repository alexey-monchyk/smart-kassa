import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    minWidth: 300,
  },
  media: {
    height: 140,
  },
  header: {
    marginBottom: 5,
  },
  descriptionItem: {
    marginTop: 10,
  },
});

export default useStyles;
